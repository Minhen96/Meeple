services:
  - type: web
    name: meeple-backend
    runtime: java
    rootDir: backend
    buildCommand: chmod +x ../gradlew && ../gradlew build -x test
    startCommand: java -jar build/libs/*.jar
    envVars:
      - key: JAVA_VERSION
        value: 21
